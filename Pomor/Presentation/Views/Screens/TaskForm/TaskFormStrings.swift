enum TaskFormStrings {
    enum Label {
        static let taskTitle = "Session Title"
        static let duration = "Duration (minutes)"
        static let icon = "Select Icon"
    }

    enum Placeholder {
        static let taskTitle = "What do you need to focus on?"
    }

    enum Validation {
        static let titleRequired = "Title is required"
        static let invalidDuration = "Invalid duration"
    }
}
